name: CI

on: push

jobs:
    DEPLOY:
        runs-on:  ubuntu-latest
        steps:
            - name: Get-Repository-Copy
              uses: actions/checkout@v2
              
            - name: build
              uses: actions/action@v1

name: CI

on: push

jobs:
    DEPLOY:
        runs-on:  ubuntu-latest
        steps:
            - name: Get-Repository-Copy
              uses: actions/checkout@v2
              
            - name: Content-1
              run: ls
              
            - name: build
              uses: ./

            - name: Content-2
              run: ls
